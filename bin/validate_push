#!/bin/sh

# Prevent pushing to master by mistake. develop is allowed (Converra deploy branch).

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "master" ]; then
  echo "You can't push directly to master branch"
  exit 1
fi
